/**
 * Represents a game mode with its configuration.
 * Currently only "Classic" mode is available.
 */
export type GameMode = {
  /** Name of the game mode (e.g., "Classic") */
  name: string;
  /** Description of the game mode */
  description: string;
};
